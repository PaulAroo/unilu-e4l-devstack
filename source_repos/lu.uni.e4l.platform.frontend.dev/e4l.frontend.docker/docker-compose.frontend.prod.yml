services:
  e4l-frontend-prod:
    restart: always
    image: ${CI_REGISTRY_IMAGE}
    container_name: e4l-frontend-prod
    ports:
      - "8882:80"
    volumes:
      - /opt/e4l-news:/usr/share/nginx/news
    environment:
      - PUBLIC_PATH=
      - API_URL=http://e4l-backend-prod:8080/e4lapi
      - TZ=Europe/Luxembourg
    networks:
      - e4l-db-net

networks:
  e4l-db-net:
    external: true